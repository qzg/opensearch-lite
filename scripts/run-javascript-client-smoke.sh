#!/usr/bin/env bash
set -euo pipefail

echo "JavaScript OpenSearch client smoke is intentionally ignored by default."
echo "Install @opensearch-project/opensearch, start opensearch-lite, then adapt docs/driver-examples.md as needed."
