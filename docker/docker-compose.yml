services:
  mainstack-search:
    build:
      context: ..
      dockerfile: docker/mainstack-search.Dockerfile
    command:
      - --listen
      - 0.0.0.0:9200
      - --allow-nonlocal-listen
      - --allow-insecure-non-loopback
      - --ephemeral
    ports:
      - "127.0.0.1:9200:9200"
